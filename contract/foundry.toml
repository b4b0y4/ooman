[profile.default]
src = "src"
out = "out"
libs = ["lib"]
remappings = [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/"
]
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
mainnet = "https://eth-mainnet.g.alchemy.com/v2/${ALCHEMY_KEY}"
op = "https://mainnet.optimism.io"

[etherscan]
mainnet = { key = "${ETHERSCAN_KEY}" }
op = { key = "${OPTIMISTIC_ETHERSCAN_KEY}" }
